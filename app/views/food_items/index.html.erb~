<h1>List of stuff in your fridge</h1>

<%= link_to 'New item', new_food_item_path %>

<table>
  <tr>
    <th>Item</th>
    <th>Expiry day</th>
    <th></th>
    <th></th>
  </tr>
  
  <% @food_items.each do |food_item| %>
    <tr>
      <td><%= food_item.title %></td>
      <td><%= food_item.expiry_date %></td>
      <td><%= link_to 'Edit', edit_food_item_path(food_item) %></td>
      <td><%= link_to 'Destroy', food_item_path(food_item), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<h2>Food ideas for you</h2>

<table>
  <tr>
    <th>Recipe</th>
    <th>Ingrediend</th>
    <th>Link</th>
  </tr>

  <% @recipes.each do |recipe| %>
    <tr>
      <td><%= recipe.title %></td>
      <td><%= recipe.ingredients %></td>
      <td><%= recipe.link %></td>
    </tr>
  <% end %>
</table>
